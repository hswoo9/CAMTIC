<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="asset">

    <!--장비관리 팝업창 (관리자) - 장비등록-->
    <insert id="setEquipmentInsert" parameterType="map">
        /*setEquipmentInsert*/
        INSERT INTO CAM_INSIDE.DJ_EQIPMN_MST
        (

            eqipmn_name, eqipmn_gbn_name, eqipmn_gbn_cmmn_cd_sn, regtr_name, regtr_sn, reg_de, create_dt, actvt_yn
        )
        VALUES
        (

            #{eqipmnName}, #{eqipmnGbnName}, #{eqipmnGbnCmmnCdSn}, #{regtrName}, #{regtrSn}, #{regDe}, NOW(), 'Y'
        )
    </insert>

    <!--공통코드 - 장비관리구분-->
    <select id="getEqipmnList" parameterType="map" resultType="map">
        /*getEqipmnList*/
        SELECT
            CM_CODE_NM AS TEXT,
            CM_CODE AS VALUE
        FROM
            CAM_COMMON.DJ_COM_CODE
        WHERE
            CM_GROUP_CODE_ID = 1
    </select>

    <!--장비등록 목록 조회-->
    <select id="getEqipmnRegList" parameterType="map" resultType="map">
        /*getEqipmnRegList*/
        SELECT
            *
        FROM
            CAM_INSIDE.DJ_EQIPMN_MST
    </select>

</mapper>